<view class='case-page'>
  <view class='list-masonry'>
    <view class='item-masonry' wx:for="{{note}}" wx:key="{{index}}" data-item="{{item}}" catchtap='goProductDetails'>
      <image src='{{item.img_url}}' mode='widthFix'></image>
      <text class='text_one'>{{item.g_name}}</text>
      <text class='text_two'>￥{{item.g_price}}</text>
    </view>
  </view>
</view>
